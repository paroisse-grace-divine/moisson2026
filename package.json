{
  "name": "grace-divine-moisson-2026",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Landing page statique — Fête de la Moisson 2026, Paroisse La Grâce Divine",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "netlify": "npm run build:site && npx --yes netlify-cli dev",
    "serve": "vite preview --outDir .",
    "build": "tsc",
    "build:site": "rm -rf dist && mkdir -p dist && cp -R index.html styles.css app.js config.js manifest.webmanifest sw.js robots.txt sitemap.xml assets dist/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.3.6"
  }
}
